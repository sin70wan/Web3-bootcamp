[profile.default]
src = "src"
out = "out"
libs = ["lib"]

# Fuzzing settings (how many random tests to run)
[fuzz]
runs = 1000  # Run 1000 random inputs

# Test settings
[profile.default.fuzz]
max_test_rejects = 65536
seed = "0x1234"